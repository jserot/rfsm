(* Template for defining the guest language information *)
(* The implementation defined in this file should match the signature [Guest.INFO]  specified in ../../../host/lib/guest.ml *)

let name = "myguest" (* TO BE MODIFIED *)
let version = "0.0"(* TO BE MODIFIED *)
