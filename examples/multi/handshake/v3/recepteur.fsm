fsm model Recepteur(
  in h: event,
  in e_rdy: bool,
  in data: t_data,
  out r_rdy: bool,
  out dout: t_data)
{
  states: R1, R2;
  vars: t: int;
  trans:
  | R1 -> R2 on h when e_rdy=1 with dout:=data, r_rdy:=0
  | R2 -> R1 on h when e_rdy=0 with r_rdy:=1;
  itrans:
  | -> R1 with r_rdy:=1;
}
