(* Template for defining the guest language specific command-line options *)
(* The implementation defined in this file should match the signature [Guest.OPTIONS]  specified in ../../../host/lib/guest.ml *)

let specs = [
    (* TO BE FILLED *)
];
