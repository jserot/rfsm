* make local (dev) version
- ./configure [--no-gui --no-doc]
- make

* publish opm package
- make opam-dist (this will create /tmp/rfsm.tar.gz)
- upload the tarball to dream.ispr-ip.fr/RFSM/disr
- cd .; opam publish https://dream.ispr-ip.fr/RFSM/dist/rfsm.tar.gz .
- follow PR notifications on Github
