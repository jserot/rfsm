1. Launch RFSM (go to the installation directory and click the application icon or use the Task Bar)
2. Open an example file (File menu > Open, then select the .fsm file in the project directory)
3. To view the state diagram(s) hit the « Diagram» button
3. To simulate the design hit the "Simu" button
4. To generate the CTask code hit the « CTask » button