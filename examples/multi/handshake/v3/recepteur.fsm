fsm model Recepteur(
     in h: event,
     in e_rdy: bool,
     in data: t_data,
     out r_rdy: bool,
     out dout: t_data)
  {
  states: R1, R2;
  vars: t: int;
  trans:
    R1 -- h.e_rdy=1 | dout:=data; r_rdy:=0 -> R2,
    R2 -- h.e_rdy=0 | r_rdy:=1 -> R1;
  itrans: | r_rdy:=1 -> R1;
  }

