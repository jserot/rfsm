(* Template for defining the guest language types *)
(* The implementation defined in this file should match the signature [Guest.TYPES]  specified in ../../../host/lib/guest.ml *)

type typ = (* TO BE FILLED *)

let no_type =  (* TO BE FILLED *)

let is_event_type t = (* TO BE FILLED *) 
let is_bool_type t =  (* TO BE FILLED *)
let mk_type_fun ty_args ty_res =  (* TO BE FILLED *)

let rec pp_typ ~abbrev fmt t = (* TO BE FILLED *)
