When building from sources

Pre-requisites
--------------

- Ocaml compiler (>=4.06) with the following OPAM packages installed:
  . dune (>= 1.11)
  . ocamlgraph
  . menhir
  . lascar 
  
Building
--------

Decompress the archive, cd to the source directory and type :

1. ./make
2. ./make install
